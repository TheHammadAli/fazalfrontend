import { Gulzar, Lato } from "next/font/google";

export const gulzar = Gulzar({
  subsets: ["latin"],
  weight: ["400"],
  display: "swap",
});

export const lato = Lato({
  subsets: ["latin"],
  weight: ["300", "400", "700", "900"],
  display: "swap",
});
